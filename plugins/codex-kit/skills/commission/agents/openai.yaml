interface:
  display_name: "Commission"
  short_description: "Create and pin one project-aware Project Lead"
  default_prompt: "Use $commission to create and pin one Project Lead from approved project truth."
policy:
  allow_implicit_invocation: false
