interface:
  display_name: "Launch"
  short_description: "Launch one branch-based Workstream from a Project Lead"
  default_prompt: "Use $launch to launch one branch-based Workstream from this pinned Project Lead."
policy:
  allow_implicit_invocation: false
